div
  script(type="@plotdb/block"): include:lsc index.ls
  div(plug="body")
    .m-edit(ld="input"): .my-2.has-tips.manual(ld="error-root")
      //- we have to use `label` for options so it will be actual clickable
      //- line-height 1.25em so multiline option (due to long content) looks better
      //- flex box on label to separate text from radiobox even if text wrapped
      .d-flex.g-2.flex-wrap
        label.mb-0.mr-2.clickable.d-flex(ld-each="option",style="line-height:1.25em")
          input.mr-1(ld="radio",type="radio")
          .d-flex.align-items-center
            span(ld="text")
            i.i-close.text-sm.ml-1.cps-hover.cps-remove(ld="remove")
        .clickable.rounded-sm.bg-dark.cps-add(ld="add") ＋
      .mt-2(ld="other"): label.clickable.d-flex.align-items-center
        input.mr-1(ld="other-radio",type="radio")
        .mr-2.text-nowrap(ld="other-prompt")
        //- input.form-control default to 100% width, which collapses otherprompt above.
        //- explicitly set flex-basis solves this issue. 5em = preserve at least a 5em space
        input.form-control.form-control-sm(type="text",ld="other-text",style="flex:1 0 5em")
      .hover-tip.tip-sm: div(ld-each="error")
    .m-view(ld="content")
